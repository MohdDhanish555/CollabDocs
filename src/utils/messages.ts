import * as authMessages from "./auth.json";
import * as userMessages from "./users.json";
import * as documentMessages from "./documents.json";

export const messages: any = {
  auth: authMessages,
  users: userMessages,
  docs: documentMessages,
};
